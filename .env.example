# Server
MCP_HOST=127.0.0.1
MCP_PORT=8787
MCP_PATH=/mcp

# observe | workspace | full
MCP_MODE=observe

# Colon-separated absolute roots. Defaults to ~/code:~/Documents:~/Downloads
MCP_ALLOWED_ROOTS=/home/YOUR_USER/code:/home/YOUR_USER/Documents:/home/YOUR_USER/Downloads

# Secrets remain blocked even in full mode unless explicitly enabled.
MCP_ALLOW_SECRETS=0

# Required whenever MCP_HOST is not loopback. Keep blank only for 127.0.0.1/::1/localhost.
# Secure MCP Tunnel normally connects to the loopback listener.
MCP_AUTH_TOKEN=

# Limits
MCP_MAX_READ_BYTES=1048576
MCP_MAX_OUTPUT_BYTES=262144
MCP_COMMAND_TIMEOUT_MS=120000
# Maximum duration of one development worker/check (30 minutes).
MCP_DEVELOPMENT_TIMEOUT_MS=1800000
MCP_RATE_LIMIT_PER_MINUTE=120


# State and audit
MCP_STATE_DIR=/home/YOUR_USER/.local/state/mcp-free
MCP_LOG_LEVEL=info

# Desktop helpers
YDOTOOL_SOCKET=/run/user/1000/.ydotool_socket
